services:
  frontend:
    # build: .
    image: "eddiegulled/briqpilot-frontend-frontend"
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
