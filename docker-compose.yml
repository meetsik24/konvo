version: '3'
services:
  frontend:
    build: .
    image: eddiegulled/briqpilot-frontend:latest
    ports:
      - "3000:3000"  # Match the port from Dockerfile
    restart: unless-stopped  # Auto-restart on failure